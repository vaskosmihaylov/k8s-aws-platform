apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: demo-api
          env:
            - name: LOG_LEVEL
              value: debug
