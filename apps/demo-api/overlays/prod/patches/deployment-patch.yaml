apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: demo-api
          env:
            - name: LOG_LEVEL
              value: info
